# --- Supabase (user auth lives here; API uses service role separately) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# --- Web app config ---
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001

# NOTE: All API/AI/connector secrets (OpenAI, Upstash, Google Drive, Notion,
# Supabase service-role) now live in the sibling StudySync-api/ project's
# .env file — not here.
